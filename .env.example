# 复制本文件为 .env.local，再填入真实值。
# .env.local 已被 .gitignore 忽略（*.local），不会提交到仓库。
#
# VITE_BAIDU_MAP_AK：百度地图「浏览器端」密钥
# 获取方式：https://lbsyun.baidu.com/ → 控制台 → 应用管理 → 我的应用 → 创建应用
#   1. 应用类型选择「浏览器端」
#   2. 在「Referer 白名单」里填写本地调试地址（如 127.0.0.1、localhost）和上线域名
#      —— 白名单留空表示不做限制，正式上线前建议按域名收紧
#   3. 保存后把生成的「访问应用（AK）」填到下面这一行
#
# 提示：前端密钥一定会出现在浏览器和构建产物中，无法真正保密，
# 日常靠 Referer 白名单防护；商用或高价值场景请改为后端代理签名调用。
VITE_BAIDU_MAP_AK=
